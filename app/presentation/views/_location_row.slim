li class="list-group-item d-flex justify-content-between align-items-center"
  strong
    = location.name
  span class="text-muted small font-monospace"
    = "#{location.latitude.to_f.round(4)}, #{location.longitude.to_f.round(4)}"
