div class="col"
  div class="card h-100"
    div class="card-body"
      h5 class="card-title"
        a href="/courses/#{course.id}"
          = course.name
      - if course.description && !course.description.empty?
        p class="card-text text-muted force-wrap"
          = course.description
